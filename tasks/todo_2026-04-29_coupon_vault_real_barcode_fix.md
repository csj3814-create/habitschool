# 2026-04-29 쿠폰 보관함 실사용 바코드 보정

## 목표
- 테스트로 받은 빽다방 쿠폰 바코드가 사용자 보관함에 보이지 않게 정리한다.
- 상용 발급 테스트에서 받은 실제 사용 가능한 쿠폰이 보관함에 표시되게 한다.
- PIN/바코드 같은 민감 정보는 작업 노트와 최종 답변에 남기지 않는다.

## 체크리스트
- [x] 관련 교훈과 쿠폰 데이터 모델 확인
- [x] 스테이징 `reward_redemptions` 문서 식별
- [x] 빽다방 테스트 쿠폰 숨김 또는 삭제 처리
- [ ] 상용 발급 쿠폰 표시 상태 보정
- [x] 보정 결과 검증

## 리뷰
- 스테이징 최석재 계정의 보이는 빽다방 쿠폰은 `mode: mock`, `providerResponseMessage: mock_issued`인 테스트 문서라 `hiddenByUserAt`으로 보관함에서 숨겼다.
- 스테이징/본서버 `reward_redemptions`에는 이전 상용 발급 샘플 쿠폰 문서가 없었다.
- 기프티쇼 쿠폰 상세조회 API는 `TR_ID`가 필수라, 이전 실발급 테스트의 `TR_ID` 또는 PIN/바코드가 없으면 기존 샘플을 공급사에서 역조회해 복원할 수 없다.
- 검증 결과 스테이징 최석재 계정의 현재 보이는 쿠폰 수는 0건이다.

## 2026-04-29 추가 조치
- 사용자 교환 버튼이 `교환 준비 중`으로 막힌 원인은 비즈머니 30,000원 운영 하한선이 사용자 교환 가능 여부까지 막고 있었기 때문이다.
- 운영 하한선은 관제 정보로만 남기고, 실제 기프티쇼 잔액이 상품 매입가보다 부족한 경우에만 사용자 교환을 막도록 수정했다.
- 주문번호 `20260429657294`는 기프티쇼 상세조회 API에서 `TR_ID`가 필수라 주문번호만으로 PIN/바코드를 반환하지 않았다. 로컬 작업 노트/세션/임시파일에서도 해당 주문의 `TR_ID` 또는 PIN은 발견되지 않았다.
