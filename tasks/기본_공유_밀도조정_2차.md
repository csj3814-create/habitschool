# 2026-04-04 기본 공유 밀도 조정 2차
> **상태**: 완료

## 목표
- 기본 공유 정책은 유지하되, 첫 화면에서 보이는 정보량을 줄여 더 빨리 읽히게 만든다.
- 공유 설정 문구와 갤러리 카드 메타를 짧게 정리한다.

## 반영 내용
- 공개 안내 문구를 더 짧게 줄였다.
- 공유 카드 공개 설정 요약을 `기본 공유 중 · 숨길 정보만 선택` 형태로 압축했다.
- 공유 카드 토글 라벨을 `이름`, `날짜`, `포인트`, `마음 글`로 줄였다.
- 갤러리 카드 날짜를 `MM.DD` 칩으로 축약하고 포인트 배지를 더 작게 정리했다.
- 갤러리 피드 설명 문구도 필터별로 더 짧게 바꿨다.

## 검증
- `npm test` 통과 (`117 passed`)
- `node --check` 대체 `.mjs` 문법 확인 통과

## 다음 후보
- 기본 공유 상태에서 친구 버튼과 메뉴 버튼 밀도 더 줄이기
- 공개 설정 패널을 칩형 토글로 더 간결하게 바꾸기
- 갤러리 카드 첫 줄의 관계 칩/스트릭 우선순위 다시 정리하기
