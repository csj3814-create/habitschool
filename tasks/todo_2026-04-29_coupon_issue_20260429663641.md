# 2026-04-29 쿠폰 발급 확인 20260429663641

## 목표
- 주문번호 `20260429663641` 쿠폰이 정상 발급됐는지 확인한다.
- 기프티쇼 API 상세조회와 앱 저장값의 유효기간을 대조한다.
- API 발급 쿠폰의 유효기간이 30일인지 60일인지 명확히 정리한다.

## 체크리스트
- [x] 관련 교훈과 현재 변경 상태 확인
- [x] Firestore에서 주문번호와 TR_ID 확인
- [x] 기프티쇼 API 상세조회 결과 확인
- [x] 카탈로그/코드의 유효기간 계산 기준 대조
- [x] 결과 기록

## 리뷰
- production에는 주문번호 `20260429663641` 기록이 없고, staging `reward_redemptions`에서 `issued`/`live` 상태의 메가MGC커피 아메리카노 쿠폰으로 확인했다. 앱 DB에는 PIN이 저장되어 있었다.
- 기프티쇼 쿠폰 상세조회 API는 `code=0000`, `resCode=0000`, `resMsg=정상처리`, `sendStatusCd=발송완료`, `validPrdEndDt=20260529235959`를 반환했다.
- 실제 만료일은 `2026-05-29 23:59:59 KST`이며 UTC 저장값은 `2026-05-29T14:59:59.000Z`다. 60일이 아니라 30일이 맞다.
- 기존 저장값은 seeded catalog의 60일 fallback을 사용해 `2026-06-28T13:01:36.852Z`로 잘못 들어가 있었다. 현재 주문 문서는 공급사 API 기준 만료일로 보정했다.
- `functions/reward-market.js`에서 기프티쇼 중첩 응답 `result[0].couponInfoList[0].validPrdEndDt`를 읽고 `YYYYMMDDHHmmss`를 한국시간 기준 ISO로 변환하도록 수정했다.
- 검증: `npm test`, `npx esbuild js/app.js --bundle --format=esm --platform=browser --outfile=$env:TEMP\habitschool-app-check.js`, `git diff --check` 통과.
