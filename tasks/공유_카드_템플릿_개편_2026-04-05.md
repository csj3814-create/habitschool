# 2026-04-05 공유 카드 템플릿 렌더러 개편
> **상태**: 완료

## 작업
- [x] DOM 캡처 기반 공유 카드 구조를 정사각형 템플릿 렌더러 기준으로 재설계
- [x] `정돈형 / 겹침형 / 포커스형` 템플릿 선택 UI 추가
- [x] Storage URL을 SDK `getBlob` 우선으로 읽어 캔버스 렌더링에 쓰도록 보강
- [x] 미리보기와 실제 공유 파일이 같은 렌더러를 타도록 통합
- [x] 테스트, 푸시, staging 배포까지 마무리

## 결과
- 공유 박스를 DOM 조합 포스터 대신 `정사각형 캔버스 렌더링 결과 이미지`로 바꿨다.
- 템플릿 선택 버튼을 추가해서 같은 기록이라도 `정돈형 / 겹침형 / 포커스형`으로 바로 바꿔 볼 수 있게 했다.
- Firebase Storage 다운로드 URL은 `getBlob(ref(storage, path))` 우선으로 읽도록 해서, 외부 이미지 CORS 때문에 placeholder로 빠지던 비율을 줄였다.
- 공유 버튼을 누를 때 별도 캡처를 다시 만드는 대신, 이미 렌더된 결과 blob/file을 바로 공유하도록 바꿨다.

## 검증
- `npm test` 통과 (`117 passed`)
- `npx esbuild js/app.js --bundle --format=esm --platform=browser` 통과
- staging 배포 완료

## 다음 시작점
- 특정 기기에서 여전히 placeholder가 보이면 Storage URL 파서에 누락된 패턴을 추가한다.
- 다음 패스는 공유 모달 문구 밀도와 플랫폼 버튼 우선순위를 더 줄이는 작업이다.
