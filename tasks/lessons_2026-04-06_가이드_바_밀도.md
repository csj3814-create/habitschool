# 2026-04-06 가이드 바 밀도/모서리 교훈

- 모바일에서 접힘 상태의 헤더형 카드(`HABIT FEED` 같은 상단 가이드)는 별도 미디어쿼리에서 `border-radius: 0`으로 풀리지 않는지 같이 확인한다.
- 같은 화면 안에서 `가이드` 카드와 바로 아래 `퀘스트` 바가 공존하면, 패딩/배지/토글 높이를 비슷한 밀도로 맞춰야 두께가 튀지 않는다.
