# 2026-05-03 자산 탭 챌린지 모바일 안정화

## 목표
- 모바일 자산 탭에서 건강 습관 챌린지가 늦게 뜨는 병목을 제거한다.
- 7일 챌린지는 7/7 완료 상태에서만 성공/보상 수령 CTA를 보여준다.
- 7일 위클리 시작은 외부 지갑 없이 앱 지갑/내장 지갑 흐름으로 진행되게 한다.
- HBT 거래 기록의 챌린지 정산 내역이 중복 실패처럼 보이는 문제를 바로잡는다.

## 체크리스트
- [x] 관련 코드 위치와 현재 데이터 흐름 확인
- [x] 건강 챌린지 렌더링을 독립/캐시 우선으로 분리
- [x] 보상 수령 가능 조건 보정
- [x] 지갑 없는 챌린지 시작 경로 보정
- [x] HBT 거래 기록 표시/필터링 보정
- [x] 테스트와 번들 검증

## 검증
- 통과: `npm test`
- 통과: `npx esbuild js/app.js --bundle --format=esm --platform=browser --outfile=$env:TEMP\habitschool-app-check.js`
- 통과: `git diff --check`

## 리뷰
- 자산 탭의 챌린지 카드는 사용자 문서/캐시만으로 먼저 그리고, 일자별 로그 보정은 백그라운드에서만 반영하도록 분리했다.
- 7일차 당일에는 7/7을 채운 경우에만 `성공!/보상 수령` 상태로 전환하고, 6/7 같은 부분 달성은 다음날 이후 정산 가능 상태로만 넘어가게 했다.
- 외부 지갑 연결 정보가 남아 있어도 활성 provider가 없으면 앱 지갑을 복원해 챌린지를 시작하도록 바꿨다.
- 0 HBT 실패 정산 로그는 앞으로 만들지 않고, 과거 잘못 만들어진 기록은 HBT 거래 내역에서 숨긴다.
