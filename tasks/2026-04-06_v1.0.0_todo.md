# 2026-04-06 v1.0.0 본서버 준비
> **상태**: 완료

## 작업
- [x] 현재 `main` 기준 배포 후보 코드와 로컬 전용 변경을 분리 점검
- [x] `v1.0.0` 릴리즈 노트와 본서버 체크리스트를 한글 문서로 정리
- [x] `changelog.html`과 release 문구를 본서버 기준으로 최종 검토
- [x] 로컬 캐시 / 로컬 설정 / 임시 파일을 정리해 worktree를 배포 기준으로 정돈
- [x] 최종 검증 후 본서버 배포 순서 제안

## 메모
- 이번 버전명은 `v1.0.0`
- 메인넷 전환은 이번 배포 범위에서 제외
- 본서버 배포는 staging 재확인 뒤 별도 확인을 받고 진행

## 리뷰
- 사용자 노출 changelog를 `v1.0.0 / v0.9.9 / v0.9.8` 기준으로 정리했다.
- 본서버 배포용 릴리즈 노트, 정리 메모, 체크리스트를 별도 문서로 정리했다.
- `.claude/settings.local.json`, `.firebase/hosting..cache`는 저장소 추적에서 제외하는 방향으로 정리했다.
- 임시 메모성 스크립트 파일은 제거해 워크트리 잡음을 줄였다.
- 검증 결과:
  - `npm test` 통과 (`117 passed`)
  - `npx esbuild js/app.js --bundle --format=esm --platform=browser`
  - `npx esbuild js/blockchain-manager.js --bundle --format=esm --platform=browser`
  - `node --check functions/index.js`
- 다음 순서:
  - `git add` + `git commit`
  - `git push origin main`
  - 사용자에게 본서버 배포 확인 요청
  - 확인 후 `firebase deploy --only hosting,functions`
