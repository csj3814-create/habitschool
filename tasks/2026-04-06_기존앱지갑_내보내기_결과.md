# 2026-04-06 기존 앱 지갑 내보내기 결과

## 반영 내용

- 자산 탭 `블록체인 지갑` 카드에 `기존 앱 지갑 내보내기` 버튼 추가
- 기존 v2 앱 지갑이 있는 사용자만 버튼과 안내 문구 노출
- 경고 모달에서 개인키를 기본 숨김 상태로 유지
- `1회 보기`를 눌렀을 때만 복호화 후 개인키 표시
- `복사`는 표시 이후에만 가능
- MetaMask / Trust Wallet 가져오기 순서를 모달 안에 함께 제공
- 모달을 닫으면 개인키와 복사 가능 상태를 초기화

## 수정 파일

- `index.html`
- `js/main.js`
- `js/blockchain-manager.js`
- `styles.css`

## 검증

- `npm test` → `117 passed`
- `npx esbuild js/blockchain-manager.js --bundle --format=esm --platform=browser --outfile=$env:TEMP\habitschool-blockchain-check.js`
- `npx esbuild js/main.js --bundle --format=esm --platform=browser --outfile=$env:TEMP\habitschool-main-check.js`

## 메모

- 이번 작업은 기존 앱 지갑 사용자의 외부 지갑 이전 보조 기능에만 범위를 제한했다.
- staging / prod 배포는 사용자 요청 후 배포 규칙에 따라 별도로 진행한다.
