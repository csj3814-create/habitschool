# 2026-04-06 친구 챌린지 로딩/버튼 교훈

- 카드 첫 상태를 `불러오는 중`으로만 두면, 비동기 로딩이 멈췄을 때 사용자에게는 영구 로딩처럼 보인다. 로딩 후 실패/타임아웃 상태를 별도로 렌더해야 한다.
- 헤더 CTA가 비동기 함수 하나에만 기대면, 로딩 실패 시 `무반응 버튼`처럼 느껴진다. 친구 없음/요청 있음/재시도 상태에 따라 버튼 라벨과 액션을 바꿔야 한다.
- 친구/챌린지처럼 여러 단계 데이터를 읽는 카드에서는 `친구 상태 요약`을 먼저 보여주고, 챌린지 목록은 그 다음 비동기로 붙이는 편이 훨씬 안전하다.
