# 진행 중인 일

CLAUDE.md 규칙에 따라 **작업 시작 전에 여기에 적고**, 진행하면서 체크하고,
끝나면 결과를 짧게 남긴다. 완료된 일은 이 파일에서 지우고, 남길 기록이 있으면
`tasks/2026-09-04_제목.md` 형태로 따로 만든다 (지금까지 421개가 그렇게 쌓여 있다).

이 파일은 **지금 하고 있거나 곧 할 일만** 담는다. 다 끝난 일이 쌓이면 목록으로서
쓸모가 없어진다 — 2026-04 ~ 2026-09 사이 이 파일이 그렇게 800줄까지 자랐다가
아무도 안 보게 됐다.

---

## 진행 중

> ### ✅ 2026-09-10 완료 — Health Connect 걸음수 (웹 v382 / 앱 versionCode 7)
>
> - 4월 PWA-only pivot 의 킬 스위치를 걷어내고 브리지를 되살렸다 (43aeffe).
> - 앱을 열 때 헬스커넥트를 직접 읽게 했다 (7ece47b, versionCode 7).
> - 최근 앱에서 복귀할 때도 다시 읽게 했다 (d1046e5) — 런처가 스스로 끝나서
>   복귀로는 안 돌기 때문이다. 웹만 고쳐서 APK 변경 없이 메웠다.
> - 데이터 보안 선언 확인: **건강 정보·피트니스 정보 둘 다 이미 체크돼 있었다.**
>   versionCode 6 부터 위젯·타일이 헬스커넥트를 읽고 있었으니 8/27 선언에 처음부터
>   들어가 있었다. Phase B(체중·체지방·혈압·혈당)도 선언상 이미 커버된다.
> - 실기기 확인 완료. **단, 처음엔 폰에 1.0.3(versionCode 6)이 깔려 있어 네 번을
>   헛짚었다** — `tasks/lessons.md` 256번.
>
> 배포 주의: `firebase.json` 의 hosting 은 `"public": "."` 이라 배포는 커밋이 아니라
> **디스크를 올린다** (8ede6bb 교훈). 배포 전 `git status` 를 볼 것.


- [x] **Health Connect 걸음수 — 되살리기부터 비공개 테스트 출시까지** (2026-09-10)
      - [x] 4월 pivot 킬 스위치 해제 + CTA 복구 (43aeffe, 웹 v381)
      - [x] 앱 열 때 헬스커넥트 직접 읽기 (7ece47b, versionCode 7 / 1.0.4)
      - [x] 복귀 시 재동기화 — 런처가 스스로 끝나 복귀로는 안 돌기 때문 (d1046e5, 웹 v382)
      - [x] 데이터 보안 선언 확인 — **건강 정보·피트니스 정보 둘 다 이미 체크돼 있었다.**
            versionCode 6 부터 위젯·타일이 읽고 있었으니 8/27 선언에 처음부터 들어가 있었다.
      - [x] staging → 운영 배포 (웹 v382), URL 로 반영 확인
      - [x] 실기기 확인 (콜드 런치·복귀 모두 동작)
      - [x] **비공개 테스트 트랙 7 (1.0.4) 출시 완료** — 8/31 이후 10일 만의 내용 있는 업데이트
      - 헛짚은 기록: 폰에 1.0.3 이 깔린 것을 모르고 네 번 진단했다 → `tasks/lessons.md` 256

- [ ] **9/12 프로덕션 액세스 재신청** (8/29 반려)
      신청서 3단계 답변 초안 + 테스터 메일 초안: `tasks/2026-09-10_play_reapply_prep.md`
      - [ ] **하드 게이트**: 12명 이상이 직전 14일 "연속" 옵트인인지 확인.
            중간 이탈자는 14일을 처음부터 다시 센다. 걸쳐 있으면 신청을 미루는 편이 낫다
      - [ ] 테스터 안내 메일 발송 (BCC, 문의 주소 `csj38141@gmail.com`)
      - [ ] 가능하면 한 번 더 업데이트. 트랙에 쌓인 것은 8/31(빈 빌드)과 9/10 둘뿐이다
      - [ ] 검토용 계정 로그인이 지금도 되는지 재확인
      - 핵심: **2번 사유는 근거가 넘치는데 전달이 안 됐다.** 반려 이후 사용자 향
        커밋이 57건인데 TWA 라 대부분 호스팅으로 나가 Play 트랙에는 안 보인다.
        신청서에서 이 구조를 먼저 설명해야 한다

- [ ] **Phase B — Health Connect 다지표** (프로덕션 액세스 승인 후 착수)
      계획서: `C:\Users\user\.claude\plans\happy-churning-mccarthy.md`
      - **InBody 앱이 체중·체지방·기초대사율·혈압을 헬스커넥트에 쓴다**(2026-09-10 실기기
        확인). 업소용 인바디 Cloud Web API 유료 계약이 **불필요해졌다.**
      - 재다는 걸음수만 읽고 쓴다. 체성분은 안 올라오므로 제외.
      - 데이터 보안 선언에 건강 정보가 이미 있어 양식을 새로 쓸 일이 없다.
      - 착수하는 날 건강 권한 선언을 같이 접수한다(심사가 제일 긴 구간).

- [ ] **운영 v377 배포 뒤 볼 것** (2026-09-08 배포). 오늘 넣은 계측의 첫 수확이다.
      - [ ] `onboarding_gate` — `shown` 비중. `legacy_account` 가 크면 게이트가
            또 잘못 닫히고 있다는 뜻이다.
      - [ ] `auth_start` → `auth_result` 전환율, `login_mode`(popup/redirect)별.
            **차이가 곧 구글에 갔다가 돌아오지 않은 사람**이다. 웨일·삼성이
            크롬 수준으로 올라오면 리다이렉트 처방이 맞은 것이다.
      - [ ] `auth_consent_blocked` — 동의 화면에서 돌아선 사람 수. 처음 보는 값이다.
      - [ ] 한 달 뒤 `scripts/measure-growth-funnel-2026-09-07.js` 재실행.
            **가입 → 기록 1회 29.1%** 가 움직였는지가 이번 작업의 성적표다.

- [x] **챌린지가 "정산 확인 중…" 에서 멈춰 보상 수령이 안 되던 것** — 화면은 확인
      중이라고 말하면서 확인을 요청하지 않았다. 요청은 로컬 추정이 *바뀔 때* 한 번만
      나갔고, 그 한 번을 놓치면 새로고침으로도 풀리지 않았다. 이제 그 문구를 띄우는
      순간마다 재계산을 요청한다 (v363, 2026-09-04).
      - [ ] 운영 배포 후 실제 챌린지로 재현 확인

- [ ] **확산 퍼널 진단** — 초대 기계는 다 만들어져 있는데 초대 유입이 2.8%다.
      무엇을 더 만들지 정하기 전에 어디서 끊기는지 잰다.
      - [x] 측정 스크립트 (`scripts/measure-growth-funnel-2026-09-07.js`, 읽기 전용)
      - [x] 운영 실행 → `tasks/2026-09-07_growth_funnel_baseline.md` (2026-09-07)
      - [x] **판정: 병목은 공유도 잔존도 아니라 첫 기록.** 608명 중 431명(70.9%)이
            단 하루도 기록하지 않았다. 반면 3일까지 간 사람은 62%가 지금도 활성이다.
            **제품의 유지력은 멀쩡하고, 시작을 안 한다.**
      - [x] **431명이 어디서 멈췄나 — 311명(72.2%)이 "로그인 부트스트랩만".**
            로그인은 성공했고(초대코드·200P 받음) 거기서 끝났다. 로그인 실패로
            잃은 사람은 11명뿐이다. 가입 문턱이 아니라 **가입 직후가 비어 있다.**
      - [ ] GA4(`G-ZS22SSRLY6`) 8/1~ 이벤트를 baseline 8절 빈칸에 채우기
            (게스트 데모 → 가입 구간은 GA 로만 보인다)

- [ ] **온보딩 모달이 608명 중 390명(64.1%)에게 뜨지 않았다** — 판정 문서 2차 참조.
      `onboardingComplete` 를 쓰는 곳은 두 군데뿐이고(`app-core.js:24242` 는
      `primaryHabit` 을 같이 쓴다, `:24407` 은 자동), 그 차이가 390명이다. 뺄셈이다.
      - 게이트가 뒤집혀 있다: `:24376` 의 `welcomeBonusGiven` 검사가 `isPendingSignup`
        보다 먼저 있고 조건 없이 `return` 한다. 한 번 놓치면 영구히 안 뜬다.
      - 표식이 **sessionStorage 30분**짜리다 (`auth.js:183`). 리다이렉트 로그인
        (삼성 인터넷·카카오 인앱)에서 잃기 쉽다.
      - `firstRewardPending` 도 온보딩 핸들러에서만 심어서(`:24245`), 건너뛴 사람은
        나중에 기록해도 첫 보상 화면을 못 본다 (`:24156` 게이트).
      - **2026-09-06 에 적은 "한 번만 참인 조건으로 걸지 않는다" 와 같은 패턴이다.**
        조건을 휘발성 표식 → `settings.primaryHabit` 부재로 바꾸면 스스로 회복된다.
      - [x] **고쳤다 (v364, 2026-09-07, staging 배포됨).** 게이트 조건을 휘발성
            표식 → `settings.primaryHabit` 부재로 바꾸고, `welcomeBonusGiven`
            검사를 게이트에서 뺐다. 30분 만료도 없앴다. 이미 기록 중인 회원은
            `lastLogDate`/`currentStreak` 로 걸러 모달이 뜨지 않게 했다
            (닫기 버튼이 없어서 쓰고 있는 사람을 막으면 안 된다).
      - [x] `firstRewardPending` 을 가입 시점(`js/auth.js`)에 심는다 —
            **실기기 확인됨 (2026-09-07).** 마음 기록 저장 시 첫 기록 축하 화면이
            떴다: 기록 +10P / **가입 선물 +200P** / 현재 220P / 2,000P 쿠폰까지
            1,780P 남음. 온보딩을 건너뛴 회원에게 안 보이던 화면이다.
      - [x] `onboarding_gate` 이벤트 추가 — 어느 갈래로 갔는지 남는다
      - [x] **스테이징 실기기 확인됨 (2026-09-07, 삼성 인터넷 일반 탭).**
            신규 구글 계정 가입 → 온보딩 모달 표시 → 습관 선택 → 주간 미션까지
            이어졌다. v365 스탬프 확인.
      - [x] **운영 배포 완료 (v365, 2026-09-07).** hosting 만. 스탬프 확인:
            운영·스테이징 모두 habitschool-v365, `app.js?v=365`.

- [ ] **삼성 인터넷 일반 탭에서 구글 로그인이 Gmail 로 샜다** (v365, staging 배포됨)
      2026-09-07 실기기 확인: "구글로 시작하기" → 구글 계정 화면 → 안드로이드가
      그 주소를 Gmail 앱에 넘김 → 메일함에서 끝. 오류도 돌아올 길도 없다.
      - **이건 위 온보딩 문제보다 앞단이다.** 로그인을 못 끝내면 `users` 문서가
        안 생기므로 이 손실은 608명 퍼널 어디에도 안 잡힌다. 분모 밖이다.
      - ~~원인: 팝업이 새 탭을 열면서 인텐트가 가로채진다~~ — **2026-09-08 정정.
        틀렸다.** 리다이렉트로 바꾼 뒤에도 같은 증상이 났고(기본형·영문판 모두),
        그때 **탭 개수가 늘지 않았다** — 새 탭이 아니라 같은 탭 이동이었다.
        즉 코드는 리다이렉트를 타고 있었는데도 안드로이드가 가로챘다.
        **진짜 원인은 기기 설정**이었다: Gmail 앱이 `accounts.google.com` 링크를
        가져가도록 「지원되는 링크 열기」가 켜져 있으면, 브라우저가 팝업을 쓰든
        리다이렉트를 쓰든 안드로이드가 그 이동을 Gmail 로 넘긴다.
        사용자가 그 설정을 끄자 증상이 멈췄다.
      - **그래도 리다이렉트 전환은 되돌리지 않는다.** 근거가 따로 있다 —
        2026-08-12 에 리다이렉트 경로의 authDomain 을 같은 출처로 고쳐 두었고
        (94aebe9), 그 수정의 혜택을 설치 앱만 받고 일반 탭은 못 받고 있었다.
        다만 **이 변경이 Gmail 증상의 해결책이라는 주장은 취소한다.**
      - 이 조건은 2026-04-27 하루에 두 번 뒤집혔다. redirect 로 갔다가
        "돌아오면 로그인 화면으로 튕김" 때문에 되돌린 것이다. **그 튕김의 진짜
        원인(서드파티 authDomain 저장소)은 2026-08-12 에 고쳤는데(94aebe9)
        조건은 같이 안 넓혔다.** 그래서 8월 이후로도 계속 새고 있었다.
      - 고침: 삼성 인터넷이면 설치 여부와 무관하게 redirect. `resolveAuthDomain`
        이 같은 헬퍼를 쓰므로 authDomain 도 같은 출처로 함께 따라간다.
      - 전제 확인함: `/__/auth/handler` 가 staging·운영 양쪽에서 462바이트
        진짜 핸들러로 응답한다 (앱 셸이 아님).
      - [x] 실기기에서 로그인이 끝나는 것은 확인됐다 (2026-09-07). 다만 그날
            성공한 것이 리다이렉트 덕인지 그때 안드로이드가 안 가로챈 것인지는
            구분되지 않았다 — 2026-09-08 에 같은 증상이 다시 났다.
      - [x] **GA4 확인함 — 이 가설은 확인되지 않았다** (2026-09-07).
            `auth_result` 는 게스트 데모에서만 발생해 이 질문에 못 쓴다
            (`js/guest-demo.js:1045`). `record_saved` 로 다시 재니 삼성 인터넷은
            222명이 675건을 기록하고 있었다 — 막혀 있지 않다. 실기기에서 Gmail 로
            새는 것을 재현한 것은 사실이나 **규모 주장은 근거가 없다.**
            설치 앱(TWA)도 삼성 UA 라 일반 탭과 분리가 안 된다.
            → `tasks/2026-09-07_ga4_auth_by_browser.md`
      - [ ] 배포 뒤 GA 에서 `onboarding_gate` 의 `shown` 비중을 본다.
            `legacy_account` 가 크면 게이트가 또 잘못 닫히고 있는 것이다.

## 확인 필요

- **동의를 로그인 앞 → 로그인 뒤로 옮겼다** (v370, 2026-09-08).
  v369 는 증상만 막았고 순서가 여전히 틀려 있었다. 로그인 화면은 정의상 아직
  누구인지 모르는 화면이라, 거기서 받은 동의는 계정이 아니라 **브라우저**에 붙는다.
  - 이제 구글 로그인을 먼저 하고, 그 계정의 Firestore `consents` 를 보고
    필요할 때만 동의 창을 띄운다(`needsConsentRefresh`). **같은 계정은 한 번만
    묻고, 브라우저를 바꾸거나 로그아웃해도 다시 묻지 않는다.**
  - 신규·기존이 같은 관문을 지난다. 예전엔 `!isNewUser` 로 갈라져 두 경로였다.
  - 동의 창 머리말이 두 벌이다 — 첫 가입은 "시작하기 전에 동의가 필요해요",
    기존 회원은 기존의 약관 개정 안내. 처음 온 사람에게 "바뀌었어요"는 말이 안 된다.
  - 거부하면 로그아웃(문서는 남긴다, 사용자 결정 2026-09-08). 앱에는 못 들어간다.
  - v368 의 빨간 표시·토스트·`auth_consent_blocked` 를 이 창으로 옮겼다.
  - 지운 것: 로그인 화면 동의 상자, `syncSignupConsentState`,
    `restoreConsentSelection`, `buildSignupConsentRecord`, `resolveConsentSelection`,
    `bindSignupConsentListeners`, 브라우저 단위 키 2개(`habitschool-consent-accepted`,
    `habitschool-consent-selection`)와 그 헬퍼들. **리디렉트 스냅샷 문제 자체가
    사라졌다** — 동의가 리디렉트 뒤에 오므로 왕복 중에 잃을 체크박스가 없다.
  - [ ] **스테이징에서 신규 계정으로 확인**: 로그인 → 동의 창 → 동의 → 온보딩 순서
  - [ ] 기존 회원(동의 기록 있는 계정)으로 로그인 시 동의 창이 **안 떠야** 한다
  - [ ] 로그아웃 후 재로그인 시에도 안 떠야 한다 — 이게 이번 변경의 핵심이다

- **동의 게이트에서 막힌 사람이 안 보이던 것** — 고침 (v368, 2026-09-08).
  회색 버튼을 눌러도 아무 일도 안 일어났고, 이유는 `title` 툴팁에만 있어
  **모바일에서는 볼 방법이 없었다.** 개발자도 버그로 봤을 만큼 막다른 길이었다.
  - `disabled` → `aria-disabled` + `.is-consent-locked` 로 바꿔 **클릭을 받는다.**
    disabled 인 버튼은 클릭 이벤트가 아예 안 와서 이유를 말할 기회가 없다.
  - 누르면 빠진 필수 항목의 체크 칸이 빨갛게 되고, 토스트로도 몇 개인지 말하고,
    첫 항목으로 스크롤·포커스한다. 체크하면 그 줄의 빨간색만 즉시 빠진다.
  - **`auth_consent_blocked` 이벤트 추가.** 여기서 막힌 사람은 `auth_start` 에
    안 잡힌다(로그인을 시작조차 못 했으므로). 따로 세지 않으면 어떤 지표에도
    안 남는 이탈이었다.
  - 잠금 자체는 클릭 핸들러(`reportMissingConsent`)가 보장한다. 로그인 진행
    중의 진짜 `disabled` 는 그대로 둔다(`aria-busy` 로 구분).

- **Android Webview 282명 · 웨일 135명이 들어와서 아무것도 안 했다** (GA4 7/1~9/7).
  둘 다 `record_saved` 0건이고, Webview 는 데모 시작조차 0이다.
  같은 기간 전체 회원이 608명인 것과 나란히 봐야 한다.

  - **Android Webview** = 인앱 브라우저 (유입원: direct 397, facebook 64,
    threads 26, youtube 17). `js/webview-detect.js` 가 외부 브라우저로 밀어낸다.
    **다만 즉시 튕기는 모양이 아니다** — page_view 671, scroll 655,
    평균 세션 82.7초다. 넘겼다면 1~2초에 스크롤도 없어야 한다.
    핸드오프가 안 걸리는 인앱 브라우저가 있다는 뜻이고, `webview-detect.js` 의
    목록(카카오·네이버·페북·인스타·라인·`; wv)`)이 `js/pwa-install.js` 의
    `isLikelyInstallWebView` 목록보다 좁다. **스레드(threads)는 어느 쪽에도
    안 걸린다.** 두 목록이 갈라져 있는 것 자체가 문제다.
  - [x] **확인함 (2026-09-08).** 카카오톡은 바로 크롬으로 열린다. 스레드는
        "앱을 나갑니다 / 계속하기" 확인 창을 한 번 거친 뒤 크롬으로 열린다.
        즉 핸드오프는 작동한다. **82.7초의 정체는 「돌아가기」를 눌러 웹뷰에
        남은 사람들**로 보인다 — 확인 창이 한 겹 더 있는 셈이다.
  - [ ] 스레드처럼 확인 창을 거치는 경로에서 이탈이 얼마나 되는지는 아직 모른다.
        넘어가기 전에 무엇을 하러 가는지 한 줄이라도 보여 줄 값어치가 있는지 검토
  - **웨일** = 유입 1위가 네이버 블로그(94), 2위 구글 검색(72). 의도 있는
    방문자인데 가입 클릭 19건에 첫 기록 0건이다. 삼성과 같은 팝업 문제일
    가능성이 있고, 그렇다면 `shouldForceGoogleRedirectLogin` 에 웨일을 넣는 것이 답.
  - [x] **웨일(안드로이드)을 리디렉트 로그인으로 돌렸다** (v367, 2026-09-07).
        삼성과 같은 처방. 데스크톱 웨일은 팝업 유지 — 인텐트 가로채기는
        안드로이드 현상이고 데스크톱 웨일은 17세션뿐이다.
        **재현이 아니라 정황에 근거한 변경이다.** 아래 계측이 다음 확인에서
        이 판단을 검증하거나 뒤집는다.
  - [x] **일반 로그인 경로에 계측을 넣었다** (v367).
        `auth_start`(버튼을 누른 시점) + `auth_result`(성공·취소·오류) +
        `login_mode`(popup/redirect) 차원. **auth_start − auth_result 가
        곧 구글에 갔다가 돌아오지 않은 사람**이고, 그게 지금까지 어떤
        지표에도 안 잡히던 손실이다.
  - [ ] 며칠 뒤 GA 에서 `login_mode` 별 auth_start → auth_result 전환율 확인.
        웨일·삼성이 크롬 수준으로 올라오면 처방이 맞은 것이다.
  - [ ] webview 는 아직 미확인 — 아래 참조

- **첫 사진 탭이 한 번 무반응이다** (2026-09-07 제보, 삼성 인터넷).
  사진을 올리려 첫 번째로 누르면 아무 반응이 없고, 다시 눌러야 선택창이 뜬다.

  **이번 변경과 무관하다 — 예전부터 있던 증상이라고 사용자가 확인해 줬다
  (2026-09-07).** 아래는 그 판단의 근거로 남긴다.

  이번 v364/v365 변경은 온보딩 게이트·로그인 모드·GA 이벤트
  네 곳뿐이고 미디어 선택 경로는 건드리지 않았다. `checkOnboarding` 도 로그인
  시점에만 돈다(`js/auth.js:1389`, `:1443`). 반면 이 증상은 2026-05 에
  v178~v189 로 열두 번 다룬 그 증상과 모양이 같다
  (`lessons_2026-05-15_samsung_picker_permission_followup.md`:
  *"async input clicks can lose user activation"*).

  운영에서는 잘 올라가는데 스테이징에서 드러난 것은, 스테이징이 갓 만든 계정에
  방금 설치된 서비스워커(캐시 콜드)였기 때문으로 보인다. 즉 **조건이 맞으면
  운영에서도 난다** — 첫 사용·업데이트 직후처럼.

  - [ ] 재현되면 화면의 🐞 버튼으로 제보 — console 링 버퍼가 같이 실려 온다.
        지금은 추측을 좁힐 실측이 없다
  - [ ] 5월 v178~v189 에서 무엇을 시도했고 무엇이 남았는지 한 번 정리한다.
        열두 번 고쳤는데 남아 있다면 접근을 바꿔야 한다는 신호다

  **첫 기록 축하 화면과의 관계는 확인됐다.** 별개 증상이지만 한 방향으로
  연결된다: 업로드가 하나라도 실패하면 `uploadFailures.length > 0` 분기가
  축하 화면을 통째로 건너뛴다(`js/app-core.js:20705`). 사진 없는 기록으로
  저장하니 정상적으로 떴다. `firstRewardPending` 은 그때 소비되지 않으므로
  다음 성공 저장에서 다시 뜬다.

- **카카오·인스타·페북·X·블로그 공유는 초대 코드를 잃는다.** 실재하는 결함이지만
  **확산의 병목은 아니다** — 진단 결과 최근 30일 공유가 36건/7명이다. 고칠 값은
  하지만(작다), 첫 기록 전환 뒤로 순서를 미룬다.

  `publishShareCardForPreview()` 를 부르는 곳은 세 군데뿐이다
  (`js/app-core.js:21781`, `:21816`, `:21899`). 플랫폼 모달 경로
  `shareFileToAppsOrFallback()` (`js/app-core.js:21904`) 에는 어느 분기에도 없다.
  그런데 이 경로도 `getShareTargetUrl()` 로 `/c/{token}` 을 내보낸다(`:21926`).
  토큰 문서가 없으니 `shareCardPreview` 가 `!snap.exists` 로 떨어져
  **`?ref=` 없는 맨 갤러리 주소로 302** 시킨다 (`functions/runtime.js:5203-5207`).

  결과: `processReferralSignup` 미호출 → 귀속·가입자 200P·자동 친구연결·초대자 500P
  전부 없음. 초대 배너(`index.html:969`)도 안 뜬다. 카카오는 미리보기까지
  정적 이미지 고정이다 (`imageUrl: APP_OG_IMAGE_URL`, `js/app-core.js:21949`) —
  카드별 OG 를 만든 이유가 카카오에서만 그대로 살아 있다.

  `copyShareLink` 의 주석 *"링크만 보내는 경우에도 미리보기가 붙어야 한다"* 로 보아
  의도가 아니라 누락이다. 한국에서 공유는 사실상 카카오톡이다.

  규모 확인: GA `share_card_sent` 총건수 − `share_cards` 문서 수
  = 초대 코드가 빠진 채 나간 공유 건수.

## 하지 않기로 한 것

- **`getAdminEconomy` 콜드 스타트 6.5초** — `minInstances: 1` 로 없앨 수 있지만
  상시 비용이 붙는다. 자주 여는 화면이 아니라 그냥 두기로 함 (2026-09-04).
- **근력 인증을 사진으로 확대** — 지금은 영상만 근력 자리(10+5P)로 센다.
  사진만 올리는 회원은 운동 30점 중 15점이 상한이다. 이 정책은 유지하고,
  대신 안내 문구를 `걸음수·운동 이미지 10+5P, 운동 영상 10+5P` 로 바꿔
  기준이 화면에 드러나게 했다 (2026-09-04).

---

## 최근 마친 일 (2026-09-02 ~ 09-04)

기록이 필요하면 각 커밋 메시지를 본다. 여기서는 무엇이 왜 바뀌었는지만 남긴다.

- **관제탑이 브라우저로 대량 문서를 내려받던 경로를 전부 서버로 옮겼다.**
  회원 목록의 최신 지표, 포인트 지급 내역, HBT 거래. 클라이언트 SDK에는 필드
  마스크가 없어 "숫자 세 개"가 문서 전체였고, `daily_logs` 500건이 1.6MB였다.
  느린 회선에서 그 하나가 20~30초 채널을 잡으면 뒤따르는 조회가 전부 줄을 섰다.
- **Firestore 전송 방식을 auto-detect로 되돌렸다.** `experimentalForceLongPolling`
  으로 바꿨다가 유휴 채널 7개가 25~30초씩 연결을 물고 있는 것을 확인하고 원복.
  근거로 삼은 측정(조회 한 건의 지연)이 잘못된 종류였다 — `tasks/lessons.md` 참고.
- **탭이 조용히 멈추지 않게 했다.** 제한시간 없는 `await` 를 없애고, 실패하면
  콘솔이 아니라 화면에 이유와 `다시 시도` 를 띄운다. 이걸 먼저 했으면 위 원인을
  훨씬 빨리 찾았을 것이다.
- **`/admin` 등 8개 경로가 1시간 캐시되던 것을 고쳤다.** `cleanUrls` 때문에
  `**/*.html` 규칙이 확장자 없는 경로에 안 걸리고 있었다.
- **신고 관리 제보 카드에서 바로 답장을 보낼 수 있게 했다.**
- **영상 압축 1차 관문을 8MB → 5MB로 낮췄다** (운영 표본 500개 기준).
- **`sampleExerciseVideoSizes` 를 지웠다.** 압축 기준을 정하려고 만든 일회성 조사용
  콜러블. 기준이 정해진 뒤 관제탑 버튼·헬퍼·export 를 걷어내고, 배포된 함수도
  양쪽 프로젝트에서 삭제했다 (2026-09-04, 승인 후).

---

## 확산 진단에서 나온 수정 (2026-09-06 ~ 09-08)

"어떻게 하면 사람들이 알아서 권할까"로 시작해 측정부터 했다. 병목은 공유가 아니라
**활성화**였다 — 회원 608명 중 431명(70.9%)이 한 번도 기록하지 않았고, 그중 311명은
온보딩 모달을 아예 본 적이 없었다. 그래서 로그인 → 동의 → 온보딩 경로를 고쳤다.

### 끝난 것

- **온보딩 게이트** — 30분짜리 휘발성 표식 대신 서버에 남는 불일치(습관을 골랐나)로
  띄운다. 리다이렉트·탭 종료로 표식을 잃으면 모달이 영영 안 뜨던 문제.
- **동의 순서** — 로그인 화면(브라우저 단위)에서 로그인 이후(계정 단위)로 옮겼다.
  같은 계정은 한 번만 받는다. 로그아웃 후 재로그인에 또 묻던 문제.
- **회색 로그인 버튼** — 눌리지 않는 대신 필수 항목을 빨갛게 표시하고 토스트를 띄운다.
  `disabled` 버튼은 클릭 이벤트가 없어 스스로를 설명할 수 없다.
- **웨일 회원 135명이 로그인 버튼을 못 보고 있었다.** 인앱 브라우저 판정이 세 파일에
  복사돼 있었고 웨일이 그중 둘에 들어 있었다. 판정을 `js/browser-detect.js` 한 곳으로
  모으고, "인앱처럼 보이나" 대신 **"알려진 독립 브라우저인가"를 먼저** 묻게 뒤집었다.
  2026-09-10 웨일 실기기에서 로그인 확인.
- **BSC Mainnet / BSC 메인넷 표기 통일** → BSC 메인넷.
- **보상 남은 기간 안내** — '최근 7일 평균 기준 약 1253일' 을 하루 상한(80P) 기준으로
  바꿨다. 평균은 기록하지 않은 날을 0으로 같이 나눠서, 막 시작한 사람에게 가장 큰
  숫자를 보여주고 있었다.

### 배포 후 확인할 것 (GA)

- `auth_browser_blocked` — 인앱 판정으로 로그인이 막히는 사람이 아직 있나. **0에 가까워야 한다.**
- `onboarding_gate` 노출 비율 — 신규 가입자 대비 모달을 실제로 본 비율
- `auth_start` → `auth_result` (`login_mode` 별) — 로그인 성공률
- `auth_consent_blocked` — 동의에서 막혀 나가는 사람 수
- 한 달 뒤 `scripts/measure-growth-funnel-2026-09-07.js` 재실행 — 기준선은 활성화 29.1%

### 남은 판단

- **첫 쿠폰 목표 2,000P가 적절한가.** 하루 상한 80P + 3일 챌린지 30P 기준으로
  0P에서 약 22~25일이다. "7~8일에 커피 한 잔"을 만들려면 목표를 500P대로 낮추거나
  초반 보너스를 얹어야 한다. 마켓 쿠폰 가격을 건드리는 일이라 결정 대기.
