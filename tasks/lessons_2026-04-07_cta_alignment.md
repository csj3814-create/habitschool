## 2026-04-07 CTA alignment

- Fixed bottom CTAs, chat banner, and install banner must use the same width model as `.app-container`.
- Do not center them with viewport-wide wrappers and larger inner widths.
- If desktop alignment looks left-shifted, compare the fixed bar width against `.app-container` width first.
