# 2026-04-06 외부 지갑 전환 결과

## 목표
- 앱 내장형 자동 지갑 생성 중심 흐름을 줄이고, 사용자가 직접 연결하는 외부 지갑 중심 구조로 전환
- `MetaMask / Trust Wallet / WalletConnect` 연결형 구조로 정리
- 본서버 배포 전 `main` 대비 변경 범위와 남은 준비 사항 정리

## 이번 작업에서 반영된 내용

### 1. 외부 지갑 우선 상태 모델
- 사용자 문서에 아래 외부 지갑 필드를 사용하도록 정리
  - `externalWalletAddress`
  - `walletConnectionMode`
  - `walletProviderType`
  - `walletChainId`
  - `walletConnectedAt`
- 자산 탭은 자동 생성 지갑 대신 `연결된 외부 지갑`을 우선 표시
- 내부 v2 지갑 데이터가 이미 있는 사용자는 fallback 표시만 허용

### 2. 지갑 연결 UX
- 자산 탭에 `MetaMask 연결`, `Trust Wallet 연결`, `WalletConnect 연결`, `연결 해제` UI 정리
- 연결 상태 문구, 주소 표시, 버튼 disabled 상태를 외부 지갑 상태 기준으로 갱신
- 로그인 직후와 자산 탭 진입 시 외부 지갑 상태를 우선 복원

### 3. 온체인 챌린지 흐름
- 30일 챌린지 시작 시 내부 지갑 고정이 아니라 `현재 연결된 지갑` 기준으로 처리
- 잔액 확인, 가스 보충, 컨트랙트 호출 모두 연결 지갑 주소 기준으로 정리

### 4. 서버 / 보안 규칙
- `functions/index.js`에서 지갑 주소 조회 시 `externalWalletAddress`를 우선 사용
- `firestore.rules`에 외부 지갑 필드 whitelist 추가

## 실제 수정 파일
- `C:\SJ\antigravity\habitschool\firestore.rules`
- `C:\SJ\antigravity\habitschool\functions\index.js`
- `C:\SJ\antigravity\habitschool\index.html`
- `C:\SJ\antigravity\habitschool\js\app.js`
- `C:\SJ\antigravity\habitschool\js\auth.js`
- `C:\SJ\antigravity\habitschool\js\blockchain-manager.js`
- `C:\SJ\antigravity\habitschool\js\main.js`
- `C:\SJ\antigravity\habitschool\styles.css`

## 검증
- `npm test`
  - 결과: `117 passed`
- `npx esbuild js/app.js --bundle --format=esm --platform=browser --outfile=%TEMP%\\habitschool-app-check.js`
  - 결과: 통과
- `npx esbuild js/blockchain-manager.js --bundle --format=esm --platform=browser --outfile=%TEMP%\\habitschool-blockchain-check.js`
  - 결과: 통과
- `node --check C:\SJ\antigravity\habitschool\functions\index.js`
  - 결과: 통과

## 현재 상태 요약

### 완료
- MetaMask 연결형
- Trust Wallet 연결형
- 외부 지갑 주소 중심 UI / 서버 / 규칙 정리

### 미완료
- WalletConnect 실제 연결

## WalletConnect가 아직 미완료인 이유
- 현재 저장소에는 `WalletConnect / Reown` 라이브러리 설정이 없음
- `projectId` 설정이 전혀 없음
- 따라서 지금 버튼은 실제 세션 연결이 아니라 안내용 fallback 상태임

## 본서버 배포 전 남은 준비
1. `WalletConnect`를 반드시 지원하려면 Reown `projectId`를 발급받아 설정
2. staging에서 MetaMask / Trust Wallet 실사용 연결 확인
3. 본서버용 지갑 연결 문구, 도움말, 오류 메시지 최종 확인

## worktree와 main 비교 메모
- 현재 브랜치: `main`
- `git diff --stat main` 기준, 이번 외부 지갑 전환에 직접 관련된 핵심 파일은 위 8개
- 다만 worktree에는 아래처럼 이번 작업과 무관한 기존 변경도 남아 있음
  - `.claude/*`
  - `.firebase/hosting..cache`
  - 일부 `tasks/*.md`
  - 기타 untracked 파일

## 정리 판단
- 외부 지갑 전환 코드는 검증 가능한 수준으로 반영됨
- 하지만 `worktree를 완전히 깨끗하게` 만들려면, 이번 작업 외 기존 변경 파일을 어떻게 처리할지 먼저 구분해야 함
- 따라서 본서버 배포 전에는 `이번 작업 관련 파일만 선택적으로 커밋/배포`하는 방식이 가장 안전함
